export function ProductsShow({ product }) {
  return (
    <div>
      <h1>Product information</h1>
      <p>Name: {photo.name}</p>
      <p>Image Url: {photo.url}</p>
      <p>Price: {photo.price}</p>
      <p>Description: {photo.description}</p>
    </div>
  );
}